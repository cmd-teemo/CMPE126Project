/*#ifndef NODE_H
#define NODE_H

#include <string>
#include "player.h"


// Node structure for a binary tree
struct Node {
    Player* value;
    Node* left;
    Node* right;

    Node(Player* val)
        : value(val), left(nullptr), right(nullptr) {}
};

#endif // NODE_H
*/